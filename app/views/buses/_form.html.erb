<%= render("layouts/errors", :object => @bus) %>

<%= form_for [@line, @bus] do |f|  %>
  <div class="form_group btn-align">
    <%= f.label(:name, 'Bus Name:') %>
    <%= f.text_field(:name, class: 'form-control btn-align') %>
  </div>
  <%= f.submit(:class => 'btn btn-primary') %>
<% end %>
